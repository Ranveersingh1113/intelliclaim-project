services:
  - type: web
    name: intelliclaim-frontend
    env: static
    plan: starter
    buildCommand: npm run build
    staticPublishPath: ./build
    envVars:
      - key: REACT_APP_API_URL
        value: https://your-backend-domain.onrender.com  # Update with your backend URL
    autoDeploy: true