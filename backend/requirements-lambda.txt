# AWS Lambda requirements for IntelliClaim backend
# Optimized for Lambda deployment with smaller package size

# Core FastAPI dependencies
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
requests==2.32.3

# AWS Lambda adapter
mangum==0.17.0

# Document processing (pure Python, no Rust dependencies)
python-docx==1.1.0
PyPDF2==3.0.1

# AI/ML dependencies (lighter versions for Lambda)
langchain==0.2.0
langchain-community==0.2.0
langgraph==0.2.21
pydantic>=2.8.0

# AI APIs
google-generativeai==0.5.0

# Vector database (lighter version)
chromadb==0.5.5

# Lightweight embedding models (faster to install)
sentence-transformers==2.2.2

# Remove gunicorn (not needed for Lambda)
