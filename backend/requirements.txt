# AWS-optimized requirements for IntelliClaim backend
# Production-ready dependencies for cloud deployment

# Core FastAPI dependencies
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
requests==2.32.3

# Document processing
python-docx==1.1.0
PyPDF2==3.0.1

# AI/ML dependencies
langchain==0.2.0
langchain-community==0.2.0
langgraph==0.2.21
pydantic>=2.8.0

# AI APIs
openai>=1.0.0
tiktoken>=0.5.0

# AWS SDK and services
boto3>=1.34.0
botocore>=1.34.0
asyncpg>=0.29.0  # PostgreSQL async driver
psycopg2-binary>=2.9.9  # PostgreSQL sync driver (fallback)

# Vector database - pgvector support
pgvector>=0.2.4
chromadb>=0.4.0  # ChromaDB for vector storage

# Embedding models (CPU-only, optimized for smaller image size)
# Using CPU-only PyTorch to reduce image size from ~7GB to ~2GB
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
transformers==4.40.0
tokenizers==0.19.1
safetensors==0.4.1
huggingface_hub==0.19.4
numpy==1.26.4

# Production dependencies
gunicorn==21.2.0
prometheus-client==0.19.0  # Metrics
structlog==23.2.0  # Structured logging

# Security and monitoring
cryptography==41.0.7
python-multipart>=0.0.7  # File upload support (required by FastAPI)

# Development and testing (optional, can be removed in production)
pytest==7.4.0
pytest-asyncio==0.21.1
httpx==0.25.2  # Async HTTP client for testing
