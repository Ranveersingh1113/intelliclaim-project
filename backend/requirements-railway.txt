# Railway-optimized requirements for IntelliClaim backend
# These versions are tested and optimized for Railway deployment

# Core FastAPI dependencies
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-dotenv==1.0.1
requests==2.32.3

# Document processing (pure Python, no Rust dependencies)
python-docx==1.1.0
PyPDF2==3.0.1

# AI/ML dependencies (Railway-optimized versions)
langchain==0.2.0
langchain-community==0.2.0
langgraph==0.2.21
pydantic>=2.8.0

# AI APIs
google-generativeai==0.5.0

# Vector database (Railway-compatible version)
chromadb==0.5.5

# Lightweight embedding models (Railway-optimized)
sentence-transformers==2.2.2

# Additional dependencies for Railway
gunicorn==21.2.0
